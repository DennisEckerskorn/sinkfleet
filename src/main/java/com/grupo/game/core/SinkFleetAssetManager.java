package com.grupo.game.core;

import com.grupo.engine.core.AssetManager;

/**
 * Manages the assets specific to the Sink Fleet game.
 */
public class SinkFleetAssetManager extends AssetManager {

    /**
     * Loads all the assets required for the Sink Fleet game.
     * Currently, this method is empty, indicating that no assets are loaded.
     * To load assets, you can use the `loadSprite` method inherited from the `AssetManager` class.
     */
    @Override
    public void loadAll() {
        //loadSprite("battleship", "images/battleship.png");
    }

}
